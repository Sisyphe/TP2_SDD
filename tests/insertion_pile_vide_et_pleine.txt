#include "stack.h"

int main()
{
    stack_t * stack = createStack(3);

    pushStack(stack, 1);
    pushStack(stack, 2);
    pushStack(stack, 3);
    pushStack(stack, 4);

    printStack(*stack);
    
    return 1;
}